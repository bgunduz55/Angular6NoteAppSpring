export class Note {
    id: string;
    username: string;
    note: string;
    oldNote: string;
    createdAt: string;
    updatedDate: string;
    editing: boolean;
    canSave: boolean;
    constructor() {
    }
}
